const texto1 = 'Aqui temos uma string normal';

const texto2 = 'Já esta, apesar das aspas duplas ao invés das simples, também é uma string';

const numeroRegistro = 31472;
const stringTemplate = `O número que quero registrar é o ${numeroRegistro}`;

console.log(stringTemplate);